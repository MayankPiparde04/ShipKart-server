services:
  - type: web
    name: shipwise-server
    env: node
    buildCommand: pnpm install
    startCommand: pnpm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /health
